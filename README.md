# python-refresher
# Updates as of 8/28/26:
# Updated my_utils.py:
    # reads a csv dataset 
    # splits each line into list of values
    # matched rows where the column of data we are interested in matches the value we want information about
    # added default parameter result_column=1
# Updated print_fires.py
    # utilizes 'my_utils.get_columns()' to get fire emission data for the United States of America
# Created run.sh
    # created bash shell script to run 'print_fires.py'
    